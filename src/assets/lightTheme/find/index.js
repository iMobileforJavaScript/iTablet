const supermap = require('./icon_discover_supermap_light.png')
const sort_name = require('./icon_discover_name_light.png')
const sort_time = require('./icon_discover_time_light.png')
const list = require('./app_list.png')

export default {
  supermap,
  sort_name,
  sort_time,
  list,
}
