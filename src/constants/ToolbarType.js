/** 工具栏容器类型 **/
const list = 'list' // 列表
const selectableList = 'selectableList' //可选择列表，每行左方多选框
const table = 'table' // 固定表格
const scrollTable = 'scrollTable' // 纵向滚动表格
const tabs = 'tabs' // 卡片式符号库容器
const symbol = 'symbol' // 符号库容器
const colorTable = 'colorTable' // 颜色表格
const horizontalTable = 'horizontalTable' // 横向滚动表格
const createPlotAnimation = 'createPlotAnimation' // 创建标绘推演
const animationNode = 'animationNode' // 态势推演
const picker = 'picker' // 选择器

export default {
  list,
  selectableList,
  table,
  scrollTable,
  tabs,
  symbol,
  colorTable,
  horizontalTable,
  createPlotAnimation,
  animationNode,
  picker,
}
